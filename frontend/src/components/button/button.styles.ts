import styled from "styled-components";

export const ButtonWrapper = styled.button`
    width: 100%;
    padding: 1rem;
    box-sizing: border-box;
    border: 1px solid #000;

    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.5rem;

    cursor: pointer;
    user-select: none;

    font-size: 2em;
    background-color: var(--utl-secondary-color);
    color: var(--bg-primary-color);
    border-radius: 1.2rem 0.6rem;
`

